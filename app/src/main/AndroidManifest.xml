<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.GET_PACKAGE_SIZE" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_sorting_genie_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_sorting_genie_icon"
        android:supportsRtl="true"
        android:theme="@style/SortingWidgetTheme">

        <service
            android:name="AccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE" >
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibility service"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".AppInfoTrackerService"
            android:exported="true"
            android:foregroundServiceType="shortService"
            android:permission="android.permission.FOREGROUND_SERVICE"/>

        <activity
            android:name=".TempWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Prioritize" />
        <activity android:name=".Include" />
        <activity android:name=".Exclude" />
        <activity
            android:name=".Size"
            android:exported="false"
            android:theme="@style/SortingWidgetTheme" />
    </application>

</manifest>